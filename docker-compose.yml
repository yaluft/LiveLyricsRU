services:
  app:
    build: .
    restart: unless-stopped
    environment:
      PORT: 8787
      SERVE_CLIENT: "true"
      CORS_ORIGIN: "*"
      LOG_LEVEL: info
      YT_DLP_COOKIES_PATH: /app/cookies.txt
    volumes:
      - lyrika-data:/app/.data
      # If ./cookies.txt doesn't exist on the host, Docker mounts an empty
      # directory here instead — ytdlp.ts checks isFile() before using this
      # path, so that case is harmless and cookies simply stay disabled.
      - ./cookies.txt:/app/cookies.txt:ro
    expose:
      - "8787"
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - "fetch('http://127.0.0.1:8787/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"
      interval: 30s
      timeout: 5s
      retries: 3

  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    depends_on:
      app:
        condition: service_healthy
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro

volumes:
  lyrika-data:
